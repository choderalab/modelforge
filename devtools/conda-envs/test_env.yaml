name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - h5py
  - requests
  - tqdm
  - qcelemental=0.25.1
  - qcportal
  - qcarchive

    # Testing
  - pytest
  - pytest-cov
  - codecov


    # Pip-only installs
  - pip:
     - pedantic
  #  - codecov

