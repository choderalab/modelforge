name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - qcelemental=0.25.1
  - qcportal
  - requests

    # Pip-only installs
  #- pip:
  #  - codecov

